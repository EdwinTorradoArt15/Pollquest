import { Header } from "@/components";
import { Stack } from "@mui/material";

const Cuestionarios = () => {
  return (
    <>
      <Stack spacing={3}>
        <Stack spacing={1}>
          <Header title="Cuestionarios" subtitle="Mis cuestionarios." />
        </Stack>
      </Stack>
    </>
  );
};

export default Cuestionarios;
