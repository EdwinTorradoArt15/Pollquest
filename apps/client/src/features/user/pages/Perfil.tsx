import { Header } from "@/components";
import { Stack } from "@mui/material";
const Perfil = () => {
  return (
    <>
      <Stack spacing={3}>
        <Stack spacing={1}>
          <Header title="Perfil" subtitle="Perfil del usuario." />
        </Stack>
      </Stack>
    </>
  );
};

export default Perfil;
